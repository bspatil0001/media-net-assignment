'use strict';

/**
 * @ngdoc overview
 * @name stockSocketApp
 * @description
 * # stockSocketApp
 *
 * Main module of the application.
 */
angular.module('stockSocketApp', [])
  .run(function($rootScope) {
      // $rootScope.stockData = [];
  });
